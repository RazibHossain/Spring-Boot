name: Product Service CI

on:
  push:
    paths:
      - 'Smart Logistics and Order Management System/productservice/**'

jobs:
  build:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: Smart Logistics and Order Management System/productservice

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build JAR
        run: mvn clean package -DskipTests

      - name: Login to Docker Hub
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      - name: Build Docker Image
        run: docker build -t ${{ secrets.DOCKER_USERNAME }}/product-service .

      - name: Push Docker Image
        run: docker push ${{ secrets.DOCKER_USERNAME }}/product-service